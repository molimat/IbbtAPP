import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

export default class Cultos extends Component {
  render() {
    return (
      <View>
        <Text> Cultos Page </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
